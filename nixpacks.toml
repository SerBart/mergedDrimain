# Nixpacks config for Railway
[phases.build]
cmds = ["./mvnw -DskipTests package"]

[start]
cmd = "java -Dserver.port=${PORT} -XX:MaxRAMPercentage=75.0 -Dspring.profiles.active=prod -jar target/driMain-1.0.0.jar"

